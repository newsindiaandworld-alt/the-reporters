// Deprecated: the temporary passcode auth has been replaced by NextAuth
// (see src/auth.ts, src/auth.config.ts, and src/middleware.ts).
// This file is no longer imported anywhere and is safe to delete —
// I don't have a way to remove files from your machine directly, so it's
// left here as an inert stub. Feel free to delete src/lib/auth.ts.
export {};
